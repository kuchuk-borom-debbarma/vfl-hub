package dev.kuku.vfl.hub.model.entity;

public enum LogType {
    INFO,
    WARN,
    ERROR,
    TRACE_PRIMARY,
    TRACE_PARALLEL_JOIN,
    TRACE_PARALLEL,
    TRACE_REMOTE,
    PUBLISH_EVENT,
    LISTEN_EVENT
}
